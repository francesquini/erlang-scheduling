-module(mandel).

-export([run/0]).

run() ->
	Size = 400,
	Image = calculate_image(Size),	
	io:format("Run~n",[]).

calculate_image (Size) ->
	lists:reverse(calculate_image (Size, [])).

calculate_image (Size, Acc) ->
	
	

