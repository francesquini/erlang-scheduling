#!/bin/bash

DIR=`dirname $0`
ERL=$($DIR/erl_cl)

mkdir $DIR/gen_scripts 2> /dev/null
$ERL -noshell -pa $DIR/../ebin -s ip_rounds generate_trace_script $* -s init stop >$DIR/gen_scripts/ip_rounds_trace.$1.sh
chmod a+x $DIR/gen_scripts/ip_rounds_trace.$1.sh

